关于重定义的行为
    -函数
        -普通函数(非static)：不允许(头文件中出现的函数只允许被一个源文件include一次)
        -static普通函数: 允许, 作用域在引用头文件的源文件
        -成员函数(非模板类)：不允许(头文件中出现的函数只允许被一个源文件include一次, 其它源文件可以通过extern进行使用)
        -模板类成员函数：
            允许, 模板类在实例化时才生成代码,也就是每个源文件只看到模板定义, 在使用时才生成实际的代码
    -变量
        -static变量: 允许, 作用域在引用头文件的源文件
        -全局变量(非static, const类型): 不允许(头文件中出现的变量只允许被一个源文件include一次, 其它源文件可以通过extern进行使用), 应当在头文件中使用extern声明, 在一个源文件中进行定义
        -const类型变量: 允许, 作用域在引用头文件的源文件(有资料说c语言中, 其作用域为全局, c++中为当前文件)
